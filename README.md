# LC pipeline (TSE -> MNI)

To create the environment:

`conda create -n mri_lc python=3.11 pip -y`

`conda install -c conda-forge antspyx nibabel nilearn matplotlib pandas numpy ants fsleyes`

`conda install -c conda-forge scipy=1.14`

`conda install conda-forge::tqdm`
